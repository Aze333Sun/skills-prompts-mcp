# 前端开发提示词

## 1. React 组件生成

```
你是一位资深 React 开发者。请根据以下需求创建一个 React 组件：

需求：{描述组件功能}

要求：
- 使用 TypeScript，定义完整的 Props 接口
- 使用 React Hooks（useState、useEffect 等）
- 处理 loading、empty、error 三种状态
- 支持受控/非受控两种模式
- 添加必要的 ARIA 属性确保可访问性
- 使用 Tailwind CSS 编写样式
- 导出组件及其类型

请直接输出完整代码。
```

## 2. CSS 布局调试

```
请帮我分析以下 CSS 布局问题。这段代码中 {描述问题现象}。

```css
{粘贴 CSS 代码}
```

请逐一排查：
1. 盒模型是否正确（box-sizing、margin、padding）
2. 定位方式是否合理（relative/absolute/fixed/sticky）
3. Flex/Grid 布局参数是否正确
4. 是否有 z-index 层级问题
5. 响应式断点是否按预期工作

给出具体修复代码和原因说明。
```

## 3. 组件性能优化

```
请审查以下 React 组件的性能问题：

```tsx
{粘贴组件代码}
```

从以下维度分析：
1. 不必要的重渲染（该用 memo/useMemo/useCallback 的地方）
2. 状态管理是否合理（是否该提升/下沉 state）
3. 副作用依赖是否正确
4. 大列表是否该用虚拟滚动
5. 图片/字体等静态资源加载策略

给出优化后的完整代码，标注每个改动的理由。
```

## 4. 响应式布局转换

```
请将以下桌面端布局改为响应式设计：

{粘贴 HTML/CSS 代码或截图描述}

要求：
- 移动端（< 768px）：单列堆叠布局
- 平板端（768-1024px）：双列布局
- 桌面端（> 1024px）：保持原有布局
- 使用 CSS Grid 或 Flexbox
- 字号、间距在移动端相应缩小
- 导航栏在移动端变为汉堡菜单

输出完整 HTML + CSS。
```

## 5. 前端工程化配置

```
请为 {React/Vue/Next.js} 项目生成完整的前端工程化配置，包括：

1. ESLint 配置（含 import 排序、hooks 规则）
2. Prettier 配置
3. Husky + lint-staged 配置
4. commitlint 配置（Conventional Commits）
5. tsconfig.json 严格模式推荐设置

要求每个配置都标注关键选项的含义。输出可直接使用的配置文件内容。
```

## 6. 前端安全审查

```
请审查以下前端代码的安全隐患：

```tsx
{粘贴代码}
```

重点检查：
1. XSS 风险（dangerouslySetInnerHTML、未转义的用户输入）
2. CSRF 防护是否到位
3. 敏感信息是否泄露到前端（API Key、内网地址）
4. 第三方依赖是否有已知漏洞
5. CSP 策略是否合理
6. localStorage/sessionStorage 是否存储了敏感数据

给出分级风险报告（严重/高/中/低）和修复建议。
```

## 7. 动画效果实现

```
请用 CSS + Framer Motion 实现以下动画效果：

{描述动画需求，如：卡片 hover 时微微上浮并增加阴影，点击时放大后消失}

要求：
- CSS 处理基础过渡（transition）
- Framer Motion 处理复杂交互动画
- 动画时长 200-400ms，使用 ease-out 缓动
- 考虑 prefers-reduced-motion 无障碍偏好
- 输出完整的 React 组件代码
```

## 8. 表单组件设计

```
请设计一个企业级表单组件，需求如下：

{描述表单需求：字段类型、校验规则、联动逻辑}

要求：
- 使用 React Hook Form 或 Formik
- Zod 或 Yup schema 校验
- 每个字段有独立的 loading/error/disabled 状态
- 字段联动逻辑清晰（如：选择了 A 选项才显示 B 字段）
- 提交时全局 loading + 防重复提交
- 错误时自动滚动到第一个报错字段
- 完整的 TypeScript 类型

输出组件完整代码 + 使用示例。
```

## 9. 前端测试编写

```
请为以下 React 组件编写完整的测试：

```tsx
{粘贴组件代码}
```

覆盖以下场景：
1. 正常渲染（所有 UI 元素存在）
2. 用户交互（点击、输入、提交）
3. 异步行为（loading → success / loading → error）
4. 边界情况（空数据、极长文本、特殊字符）
5. 可访问性（键盘导航、ARIA 属性）

使用 Testing Library + Jest/Vitest，输出可直接运行的测试文件。
```

## 10. 微前端架构设计

```
请为以下场景设计微前端架构方案：

业务背景：{描述：几个独立团队、各自的 tech stack、需要集成到一个平台}

请输出：
1. 技术选型（Module Federation / qiankun / micro-app / single-spa）
2. 基座应用设计
3. 子应用注册与加载机制
4. 应用间通信方案
5. 样式隔离方案
6. 公共依赖提取策略
7. CI/CD 与独立部署策略
8. 各方案的优劣对比

最后给出推荐方案及理由。
```
